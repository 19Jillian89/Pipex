Libft

![Language](https://img.shields.io/badge/language-C-blue) ![42](https://img.shields.io/badge/42-common%20core-black)

---

### 🧩 Included library
This project uses my [Libft](./libft/) — a custom implementation of basic C functions.

___

📁 Files

```
libft/
├── ft_strdup.c
├── ft_split.c
├── ft_substr.c
├── ft_strlen.c
├── ft_putstr_fd.c
├── ft_strnstr.c
├── ft_strjoin.c
├── libft.h
└── Makefile
```


🔧 Functions Description

| Function       | Description                                |
| -------------- | ------------------------------------------ |
| `ft_strlen`    | Returns the length of a string             |
| `ft_strdup`    | Duplicates a string (malloc)               |
| `ft_strjoin`   | Joins two strings into a new one           |
| `ft_strnstr`   | Locates a substring in a string            |
| `ft_substr`    | Extracts a substring from a string         |
| `ft_split`     | Splits a string using a delimiter          |
| `ft_putstr_fd` | Writes a string to a given file descriptor |

---

